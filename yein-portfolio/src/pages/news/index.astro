---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('news', ({ data }) => !data.draft)).sort(
  (a, b) => b.data.date.valueOf() - a.data.date.valueOf()
);
---

<BaseLayout title="News Clips" description="A collection of news stories Yein enjoys reading">
  <section class="wrap page-head">
    <p class="eyebrow">Scrapbook</p>
    <h1>News Clips</h1>
    <p class="page-lede">A space for news stories I found interesting, and why.</p>
  </section>

  <section class="wrap">
    {posts.length > 0 ? (
      <div class="card-grid">
        {posts.map((post) => (
          <a class="card" href={`/news/${post.slug}/`}>
            <div class="card-cover">
              {post.data.cover && <img src={post.data.cover} alt={post.data.title} />}
            </div>
            <div class="card-body">
              <span class="card-tag">{post.data.sourceName}</span>
              <h3 class="card-title">{post.data.title}</h3>
              <span class="card-date">{post.data.date.toLocaleDateString('en-US')}</span>
            </div>
          </a>
        ))}
      </div>
    ) : (
      <p class="empty-state">No news clips yet.</p>
    )}
  </section>
</BaseLayout>

<style>
  .page-head {
    padding: 48px 24px 28px;
  }
  .page-lede {
    color: var(--ink-soft);
    margin: 8px 0 0;
  }
</style>
